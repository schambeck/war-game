package com.schambeck.war.core;

import javafx.scene.shape.Box;

public class Ground extends Box {
    public Ground(double width, double height, double depth) {
        super(width, height, depth);
    }
}
